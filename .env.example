# Web server port
WEB_PORT=3001

# Password for dashboard login
AUTH_PASSWORD=your_password

# Set to 'production' for secure cookies (when behind HTTPS/Caddy)
NODE_ENV=production

# Set to 'true' if running behind a reverse proxy (Caddy/Nginx) to get correct client IPs
TRUST_PROXY=true
